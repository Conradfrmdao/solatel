{
  "name": "solatel-client",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Solatel browser client. Renders and predicts; the server decides.",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "puppeteer-core": "^25.11.0"
  },
  "dependencies": {
    "three": "^0.186.0"
  }
}
